@@ apply plugin: 'com.android.application'

+android {
+    sourceSets {
+        main.java.srcDirs += 'src/main/java'
+    }
+}

+dependencies {
+    implementation fileTree(dir: 'libs', include: ['*.jar']) // ← 향후 jar 필요시만 유지
+}